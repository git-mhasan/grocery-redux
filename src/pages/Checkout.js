import React from 'react';
import CartItem from '../layout/CartItem';

const Checkout = () => {
    return (
        <div>
            <CartItem></CartItem>
        </div>
    );
};

export default Checkout;